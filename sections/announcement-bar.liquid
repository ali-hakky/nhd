{%- if section.settings.text != blank -%}
<div class="ann">{{ section.settings.text }}</div>
{%- endif -%}

{% schema %}
{
  "name": "Announcement bar",
  "settings": [
    { "type": "text", "id": "text", "label": "Announcement text", "default": "Free shipping on orders over £500  ·  All pieces handcrafted in Egypt  ·  Cairo & London studio" }
  ]
}
{% endschema %}
