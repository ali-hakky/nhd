{%- comment -%} Generic page {%- endcomment -%}
<div class="page-wrap" style="max-width:760px">
  <nav class="breadcrumb"><a href="{{ routes.root_url }}">Home</a> / <span>{{ page.title }}</span></nav>
  <h1 class="product-title" style="margin-bottom:28px">{{ page.title }}</h1>
  <div class="rte">{{ page.content }}</div>
</div>

{% schema %}
{ "name": "Page", "settings": [] }
{% endschema %}
