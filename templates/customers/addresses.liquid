<div class="page-content">
  <h1>My Addresses</h1>
  {%- for address in customer.addresses -%}
    <div style="border:1px solid var(--br);padding:16px;margin-bottom:12px">
      <p>{{ address | format_address }}</p>
    </div>
  {%- else -%}
    <p>No saved addresses.</p>
  {%- endfor -%}
</div>
